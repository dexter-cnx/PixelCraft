name: pixelcraft
description: Offline, ultra-fast image editor powered by Flutter and Rust.
publish_to: none
version: 0.1.0+1

environment:
  sdk: '>=3.12.0 <4.0.0'
  flutter: '>=3.44.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  easy_localization: ^3.0.7
  go_router: ^17.3.0
  camera: ^0.10.6
  image_picker: ^1.1.2
  flutter_rust_bridge: ^2.12.0
  path_provider: ^2.1.5
  saver_gallery: 5.0.3
  share_plus: ^12.0.2
  shared_preferences: ^2.5.5
  dxtr_pixs_editing:
    path: packages/dxtr_pixs_editing
  dxtr_pixs_engine:
    path: packages/dxtr_pixs_engine
  dxtr_pixs_film:
    path: packages/dxtr_pixs_film
  dxtr_pixs_gpu:
    path: packages/dxtr_pixs_gpu

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.4

flutter_launcher_icons:
  android: true
  ios: true
  image_path: assets/branding/app_icon.png
  remove_alpha_ios: true
  min_sdk_android: 21

flutter:
  uses-material-design: true
  assets:
    - assets/samples/
    - assets/translations/
